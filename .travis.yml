dist: focal
addons:
  apt:
    packages:
      - perl6-tap-harness
      - perl6-zef
      - rakudo
      - unixodbc
      - cl-sql-odbc
      - libsqliteodbc
      - odbc-postgresql
      - tdsodbc
script:
  - prove6 --verbose
