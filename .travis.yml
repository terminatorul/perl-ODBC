dist: focal
addons:
  apt:
    update: true
    packages:
      - unixodbc
      - cl-sql-odbc
      - libsqliteodbc
      - odbc-postgresql
      - tdsodbc
language: perl6
perl6:
  - latest
script:
  - odbcinst -q -d
  - odbcinst -j
