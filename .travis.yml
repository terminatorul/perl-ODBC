dist: focal
addons:
  apt:
    update: true
    packages:
      - unixodbc
      - cl-sql-odbc
      - libsqliteodbc
      - odbc-postgresql
      - tdsodbc
language: perl6
script:
  - prove6 --verbose
